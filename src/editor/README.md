# Editor Module

Логика работы редактора.

## Компоненты
- `Editor.gd` — основной класс редактора
- `Tools.gd` — инструменты редактора
- `Selection.gd` — управление выделением
- `Modes.gd` — режимы работы (просмотр, редактирование)
