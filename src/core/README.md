# Core Module

Базовые классы и утилиты для всего проекта.

## Компоненты
- `BaseObject.gd` — базовый класс для всех игровых объектов
- `Utils.gd` — вспомогательные функции
- `Config.gd` — глобальная конфигурация
- `EventBus.gd` — глобальная шина событий
